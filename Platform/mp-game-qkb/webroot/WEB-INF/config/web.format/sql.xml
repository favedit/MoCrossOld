<?xml version='1.0'?>
<Syntax>
   <Format>
      <Comment color='green'/>
      <KeyWord color='blue'/>
      <Type color='#FF8000'/>
      <Function color='red'/>
   </Format>
   <Splitter>[ ,.!@#$%^&amp;*`~'":;()&lt;&gt;{}-=\+|\t\r\n]</Splitter>
   <Word>
      <KeyWord>
         ADD ALTER AND AS AUTO_INCREMENT
         BEGIN BETWEEN BODY BY
         CLOSE CLUSTER COMMENT COMMIT CONSTANT CONSTRAINT COST CREATE CURSOR
         DATABASE DECLARE DEFAULT DEFINE DELETE DROP
         EACH ELSE ELSIF END ERROR EXCEPTION EXCLUSIVE EXISTS EXIT
         FETCH FOR FOREIGN FOUND FROM FUNCTION
         IF IN INDEX INITIAL INSERT INTO IS
         LOCK LOOP
         KEY
         MODE MODIFY
         NOT NOTFOUND NOWAIT
         OF ON ONLY OPEN OR OTHERS OUT
         PACKAGE PARTITION REVERSE ROW PRAGMA PRIMARY PROCEDURE PROFILE PROMPT
         RAISE READ REFERENCES RENAME REPLACE RESOURCE RETURN ROLE ROLLBACK
         SAVEPOINT SEGMENT SELECT SEQUENCE SESSION SET SHARE SHOW SIZE SQLCODE SQLERRM
         TABLE THEN TO
         UNDEFINE UNIQUE UPDATE USING
         VALUES VIEW
         WHEN WHERE WHILE WITH
         TABLESPACE
      </KeyWord>
      <Type>
         BIGINT BINARY_INTEGER BOOLEAN
         CHAR
         DATE DATETIME
         FALSE FLOAT
         INT INTEGER
         LONG
         NULL NUMBER
         ROWTYPE
         TIMESTAMP TINYINT TRUE TYPE
         VARCHAR VARCHAR2
      </Type>
      <Function>
         ABS ACOS ADD_MONTHS ASCII ASIN ATAN AVG
         BFILENAME
         CEIL CHARTOROWID CHR CONCAT CONVERT COS COSH COUNT
         DEREF DUMP
         EMPTY_BLOB EMPTY_CLOB
         EXP
         FLOOR FOUND
         GREATEST
         HEXTORAW
         INITCAP INSTR INSTRB
         LAST_DAY LEAST LENGTH LENGTHB
         LN LOG LOWER LPAD LTRIM
         MAKE_REF MAX MIN MOD MONTHS_BETWEEN
         NEW_TIME NEXT_DAY NLS_CHARSET_DECL_LEN NLS_CHARSET_ID NLS_CHARSET_NAME NLS_INITCAP NLS_LOWER NLS_UPPER NLSSORT NOTFOUND NOW NVL
         POWER
         RAWTOHEX REFTOHEX ROUND ROWIDTOCHAR RPAD RTRIM
         SIGN SIN SINH SOUNDEX SQRT STDDEV SUBSTR SUBSTRB SUM SYSDATE
         TAN TANH TO_CHAR TO_DATE TO_MULTI_BYTE TO_NUMBER TO_SINGLE_BYTE TRANSLATE TRANSLATE TRUNC
         USING UID UPPER USER USERENV
         VARIANCE VSIZE
      </Function>
   </Word>
</Syntax>
