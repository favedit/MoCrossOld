<?xml version="1.0" encoding="UTF-8"?>

<Config>

   <!-- 类工作路径 -->
	<Define name='home.class'>${home}/build/classes</Define>

	<!-- WEB工作路径 -->
	<Define name='home.web'>${home}/webroot</Define>
	<Define name='home.web.info'>${home.web}/WEB-INF</Define>
	<Define name='home.web.config'>${home.web}/WEB-INF/config</Define>
	<Define name='home.web.data'>${home.web}/WEB-INF/data</Define>
	<Define name='home.web.store'>${home.web}/WEB-INF/store</Define>

	<!-- 临时工作路径 -->
	<Define name='home.work'>${home}/work</Define>

	<!-- 代码工作路径 -->
	<Define name='home.source.core'>${home}/src/core-java</Define>
	<Define name='home.source.web-system'>${home}/src/web-system</Define>
	<Define name='home.source.eng'>${home}/src/eng-java</Define>
	<Define name='home.source.data'>${home}/src/data-java</Define>
	<Define name='home.source.logic'>${home}/src/logic-java</Define>
	<Define name='home.source.logic.data'>${home}/src/logic-data</Define>
	<Define name='home.source.web'>${home}/src/web-java</Define>
	<Define name='home.source.web.tag'>${home}/src/web-java</Define>
	<Define name='home.source.game.editor'>${home}/src/editor-java</Define>
	<Define name='home.source.sql'>${home}/sql</Define>
   <Define name='home.deploy.store'>${store}/deploy/current</Define>

</Config>
