namespace MO.Content2d.Frame.Common
{
   //============================================================
   // <T>对齐方式。</T>
   //============================================================
   public enum ERcAlign : int
   {
      // 不对齐
      None = 0,

      // 左对齐
      Left = 1,

      // 左上对齐
      LeftTop = 2,

      // 上对齐
      Top = 3,

      // 右上对齐
      RightTop = 4,

      // 右对齐
      Right = 5,

      // 右下对齐
      RightBottom = 6,

      // 下对齐
      Bottom = 7,

      // 左下对齐
      LeftBottom = 8,

      // 中间对齐
      Center = 9,

      // 总数
      Count = 10,
   }
}
